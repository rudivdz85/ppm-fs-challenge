# Test Environment Configuration
NODE_ENV=test
PORT=5001

# Test Database - CRITICAL: Use separate test database!
DATABASE_URL=postgresql://username:password@localhost:5432/ppm_challenge_test

# Alternative test database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ppm_challenge_test
DB_USER=postgres
DB_PASSWORD=password

# JWT Configuration for testing
JWT_SECRET=test-secret-key-for-testing-only
JWT_EXPIRES_IN=24h

# Security
BCRYPT_SALT_ROUNDS=12

# Logging (disabled in tests)
LOG_LEVEL=warn
ENABLE_REQUEST_LOGGING=false

# CORS
CORS_ORIGIN=http://localhost:3000